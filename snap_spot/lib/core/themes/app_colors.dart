import 'package:flutter/material.dart';

class AppColors {
  // Màu nền chính
  static const Color background = Color(0xFFFAEBCE); // màu nền be

  // Màu chủ đạo
  static const Color primary = Color(0xFF006D77); // Xanh biển đậm
  static const Color accent = Color(0xFF83C5BE); // Xanh biển nhạt
  static const Color green = Color(0xFF215858);   // Xanh đậm

  // Màu văn bản
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF666666);

  // Màu trắng/đen tiêu chuẩn
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Màu xám dùng cho icon hoặc border
  static const Color gray = Color(0xFFB0BEC5);

  // Màu overlay đen mờ
  static const Color overlay = Colors.black54;

  // Màu shadow nhẹ
  static const Color shadow = Color(0x1A000000); // 10% đen
}
